plugins {
    id 'java'
}

allprojects{
    group 'ru.clevertec.smirnov'
    version '1.0.1'
}

subprojects{
    repositories {
        flatDir {
            dirs "${project.rootDir}/libs"
        }
        mavenLocal()
        mavenCentral()
    }
}

wrapper {
    gradleVersion "7.6"
}
